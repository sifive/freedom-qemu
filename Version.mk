# Version number, which should match the official version of the tool we are building
RISCV_QEMU_VERSION := 4.2.0

# Codeline name, which should correlate to the repo or branch of the tool we are building (riscv, rvv, ...)
FREEDOM_QEMU_SYSTEM_CODELINE := riscv

# Generation number, which should be increased when version changes, and reset to 1 when codeline changes
FREEDOM_QEMU_SYSTEM_GENERATION := 1

# Build number, which should be increased for each new build, and reset to 0 when generation or codeline changes
FREEDOM_QEMU_SYSTEM_BUILD := 1
